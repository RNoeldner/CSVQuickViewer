<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <Target Name="PostBuild" AfterTargets="PostBuildEvent">
    <Exec Command="DEL &quot;$(SolutionDir)\Application\bin\Release\net8.0-windows\*.pdb&quot;&#xD;&#xA;&quot;C:\Program Files (x86)\Inno Setup 6\ISCC.exe&quot; &quot;$(ProjectDir)InnoSetup.Net8.iss&quot;&#xD;&#xA;powershell Compress-Archive -path &quot;$(SolutionDir)\Application\bin\Release\net8.0-windows\*&quot; &quot;$(ProjectDir)..\CSVQuickViewerPortable.Net8.zip&quot;&#xD;&#xA;COPY &quot;bin\CsvQuickViewerInstaller.exe&quot; &quot;$(ProjectDir)..\CSVQuickViewerInstaller.Net8.exe&quot;" />
  </Target>

  <ItemGroup>
    <None Remove="InnoSetup.Net8.iss" />
  </ItemGroup>

  <ItemGroup>
    <Content Include="InnoSetup.Net8.iss" />
  </ItemGroup>

  <Target Name="PreBuild" BeforeTargets="PreBuildEvent">
    <Exec Command="IF EXIST &quot;$(ProjectDir)..\CSVQuickViewerInstaller.Net8.exe&quot;  (&#xA;DEL &quot;$(ProjectDir)..\CSVQuickViewerInstaller.Net8.exe&quot; &#xA;)&#xD;&#xA;IF EXIST &quot;$(ProjectDir)..\CSVQuickViewerPortable.Net8.zip&quot;  (&#xA;DEL &quot;$(ProjectDir)..\CSVQuickViewerPortable.Net8.zip&quot; &#xA;)&#xD;&#xA;" />
  </Target>

</Project>
